services:
  - type: web
    name: dpdp-shield-api
    env: docker
    region: singapore
    plan: free
    rootDir: backend
    healthCheckPath: /health
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: dpdp-db
          property: connectionString
      - key: REDIS_URL
        value: redis://dpdp-redis:6379
      - key: GEMINI_API_KEY
        sync: false

  - type: pserv
    name: dpdp-redis
    env: docker
    region: singapore
    plan: free
    repo: https://github.com/render-examples/redis.git
    branch: master

databases:
  - name: dpdp-db
    databaseName: dpdp
    user: dpdp_user
    plan: free
    region: singapore
